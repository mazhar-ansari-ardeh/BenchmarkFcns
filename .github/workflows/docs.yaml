name: Docs

on:
  push:
    branches:
      - master

permissions:
  contents: write

jobs:
  build-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          # Install the package so sphinx can import it
          pip install .
          # Install documentation tools
          pip install sphinx furo myst-parser sphinx-autodoc-typehints

      - name: Build documentation
        run: |
          sphinx-build -b html docs/source docs/build/html
          # Prevent GitHub Pages from ignoring folders starting with underscores
          touch docs/build/html/.nojekyll
          # Recreate CNAME for custom domain
          echo "benchmarkfcns.info" > docs/build/html/CNAME

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: docs/build/html
          branch: gh-pages
