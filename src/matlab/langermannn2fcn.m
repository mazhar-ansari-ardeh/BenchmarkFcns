% Computes the value of the Langermann N. 2 benchmark function.
% SCORES = LANGERMANNN2FCN(X) computes the value of the function at point X.
% LANGERMANNN2FCN is an alias for LANGERMANNFCN.
function scores = langermannn2fcn(x)
    scores = langermannfcn(x);
end
