% Computes the value of the Brent N. 1 benchmark function.
% SCORES = BRENTN1FCN(X) computes the value of the function at point X.
% BRENTN1FCN is an alias for BRENTFCN.
function scores = brentn1fcn(x)
    scores = brentfcn(x);
end
