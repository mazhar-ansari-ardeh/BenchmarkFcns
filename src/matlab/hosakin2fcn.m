% Computes the value of the Hosaki N. 2 benchmark function.
% SCORES = HOSAKIN2FCN(X) computes the value of the function at point X.
% HOSAKIN2FCN is an alias for HOSAKIFCN.
function scores = hosakin2fcn(x)
    scores = hosakifcn(x);
end
