% Computes the value of the Price N. 2 (Periodic) benchmark function.
% SCORES = PRICEN2FCN(X) computes the value of the Price N. 2 function at point X.
% PRICEN2FCN is an alias for PERIODICFCN.
function scores = pricen2fcn(x)
    scores = periodicfcn(x);
end
