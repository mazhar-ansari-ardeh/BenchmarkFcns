% Computes the value of the Schwefel 2.26 (sine-root) benchmark function.
% SCORES = SCHWEFEL226FCN(X) computes the value of the function at point X.
% SCHWEFEL226FCN is an alias for SCHWEFELFCN.
function scores = schwefel226fcn(x)
    scores = schwefelfcn(x);
end
