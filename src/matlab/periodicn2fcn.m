% Computes the value of the Periodic N. 2 benchmark function.
% SCORES = PERIODICN2FCN(X) computes the value of the function at point X.
% PERIODICN2FCN is an alias for PERIODICFCN.
function scores = periodicn2fcn(x)
    scores = periodicfcn(x);
end
