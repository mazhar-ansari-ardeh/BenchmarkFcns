% Computes the value of the Eggholder N. 2 benchmark function.
% SCORES = EGGHOLDERN2FCN(X) computes the value of the function at point X.
% EGGHOLDERN2FCN is an alias for EGGHOLDERFCN.
function scores = eggholdern2fcn(x)
    scores = eggholderfcn(x);
end
