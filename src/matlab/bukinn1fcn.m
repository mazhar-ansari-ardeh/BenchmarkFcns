% Computes the value of the Bukin N. 1 benchmark function.
% SCORES = BUKINN1FCN(X) computes the value of the function at point X.
% BUKINN1FCN is an alias for BUKINN6FCN.
function scores = bukinn1fcn(x)
    scores = bukinn6fcn(x);
end
