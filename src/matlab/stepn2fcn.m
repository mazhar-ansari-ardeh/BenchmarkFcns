% Computes the value of the Step N. 2 benchmark function.
% SCORES = STEPN2FCN(X) computes the value of the function at point X.
% STEPN2FCN is an alias for STEPFCN.
function scores = stepn2fcn(x)
    scores = stepfcn(x);
end
