% Computes the value of the Keane N. 2 benchmark function.
% SCORES = KEANEN2FCN(X) computes the value of the function at point X.
% KEANEN2FCN is an alias for KEANEFCN.
function scores = keanen2fcn(x)
    scores = keanefcn(x);
end
